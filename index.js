/*function flashingText(obj, arr, int){
  setInterval(setText(obj, arr, int), int);
  function setText(obj, arr, int){
    for(int i = 0; i < arr.lengthnewFunction(); i++){
      if(i = arr.length){
        let i = 0;
      }
      document.getElementById(obj).innerHTML = arr[i];
    }
  }
}
*/